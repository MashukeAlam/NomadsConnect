name: Deploy to server when repository gets updated
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2
      
      - name: Install SSH Keys
        # check this thread to understand why its needed:
        # https://stackoverflow.com/a/70447517
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -t rsa 62.72.7.16 >> ~/.ssh/known_hosts

      - name: Deploy via SSH
        run: ssh root@62.72.7.168 "cd /root/app/NomadsConnect && git checkout main && git pull && exit"

      - name: Cleanup
        run: rm -rf ~/.ssh
